#ifndef TIMER_H
#define TIMER_H

void timer2init(void);                 //Function to initialise timer2 which is the 5kHz timer for the current control
void timer4init(void);                 //Function to initalise timer4 which is the 200Hz timer for the motor control

#endif
